[build]
builder = "nixpacks"
buildCommand = "npm install && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && chmod a+rx /usr/local/bin/yt-dlp && /usr/local/bin/yt-dlp --version"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"