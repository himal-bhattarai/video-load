services:
  - type: web
    name: video-downloader
    runtime: node
    buildCommand: >-
      npm install &&
      pip3 install yt-dlp &&
      which yt-dlp &&
      yt-dlp --version
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000